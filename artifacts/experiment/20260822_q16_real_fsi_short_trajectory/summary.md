# 实验摘要

- 状态：PASS。
- 新能力：有界的真实 Q16 强耦合多步 owner、不可变逐步记录、结构/
  气动谱系、LEV/TEV/自由尾迹历史证据和 SHA-256 前向链。
- 成功工况：两步，`dt=0.04`，`E=1e8`，2x3 气动面元，float64 CUDA，
  separated LEV 与联合 TEV/自由尾迹始终开启。
- 收敛：两步残差 `1.9077e-8`、`1.9711e-7`，均不超过 `2e-7`。
- 历史：LEV `12 -> 24 -> 36`；自由尾迹对流 `0 -> 1 -> 2`。
- 事务：请求不存在的第三时间坐标时，保留精确两步前缀且失败步不污染
  live owner。
- 回归：176/176 通过。
- 边界：这是多步数据通路和事务验证，不是长时物理或论文精度验证。
- 下一步：扩展真实 Ptera 时域，建立 4--8 步功/能账本和历史稳定门。
