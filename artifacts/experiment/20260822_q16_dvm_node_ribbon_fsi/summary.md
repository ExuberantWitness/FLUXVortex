# 摘要

Q16 强耦合已经从历史 `hirato_ring` 夹具迁移到当前
`dvm_node_ribbon` 气动 owner 的两步开发门。LEV 始终集成，但只有
`abs(LESP)>0.11` 的展向 cell 才释放；5° 零释放和 20° 自然释放均通过。

20° 两步轨迹中，LEV 粒子为 `206 -> 413 -> 571`，自由尾迹提交为
`0 -> 1 -> 2`，最大耦合/结构/虚功残差分别为 `3.47e-9`、`7.27e-10`、
`1.12e-12`。生产载荷唯一归属 Ptera `KJ+dGamma`，涡冲量只作诊断。

受影响 GPU-only 回归为 73/73。当前结论仅覆盖两步开发门，下一节点是
4--8 步条件释放轨迹，之后才进入周期或论文复现 CASE。
