# Superseded evidence snapshot

This schema-v1 run remains preserved for audit history but is superseded by `../20260814_fluxv_v5h_r0_r1_oracle_direct_v2/`.

The numerical results were valid. Schema-v2 removes stale U/J fields from RK `pre/post` state snapshots, adds probe/nearfield and affine step-time freestream fixtures, machine-checks all configuration flags, and automatically recomputes stored metrics.
