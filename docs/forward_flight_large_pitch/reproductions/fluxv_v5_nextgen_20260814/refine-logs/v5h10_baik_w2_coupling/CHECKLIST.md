# v5h10 Baik-W2 coupling checklist

- [x] W2 case and numerical settings frozen before candidate output
- [x] two independent v4b baseline numeric replays completed
- [x] baseline source/result hash closure checked
- [x] candidate RMSE/Q1 thresholds frozen in
  `/tmp/fluxv-paper-case-20260816/BAIK_W2_PREREG.json`
- [x] source-only W2 prepass freezes steps 4--6 as
  `first -> continuous -> continuous` without reading GT
- [x] pre-execution clock audit maps one-based source steps 4--6 to
  zero-based Ptera states 3--5 before any raw candidate
- [x] v5h9 nested-lineage integrity gate passes fresh audit
- [x] one global 8-cell/9-node release-row owner passes mechanical tests
- [x] shared span edges are owned once globally
- [x] physical state has zero clone/counter/fresh-upstream particles
- [x] source/Ptera row commit occurs before inherited parent field/load work
- [x] failure leaves the third-row owner current and Ptera parent unchanged
- [x] no-GT third-row `32/64` diagnostic proves sigma positivity alone is
  insufficient (`Gamma` relative-L2 remains about 5.82%)
- [x] the full-trajectory `N=32,64,128` stability and convergence rule is
  frozen before those complete results, without core changes,
  clipping, or target-derived tuning
- [x] exploratory full three-layer `N=32,64,128` diagnosis completed without
  GT and conservatively STOPped N64 on state/probe/invariant failures
- [x] terminal decision binds the historical freeze and diagnostic hashes and
  forbids a fresh formal N64 run, N128/N256 promotion, and paper scoring
- [ ] checksummed 12-file formal convergence bundle — BLOCKED BY V5H10 STOP
- [ ] real W2 sigma/spacing formal three-row regression — BLOCKED BY V5H10 STOP
- [ ] three-layer W2 formal raw candidate — BLOCKED BY V5H10 STOP
- [ ] each layer executes one collocation feedback and four load batches
- [ ] each layer executes exactly one atomic macro transport containing the
  frozen N independent three-stage rVPM substeps
- [ ] all live material subdivisions and frontier nodes share those exact
  stage-pre transport fields
- [ ] Kelvin residual <= 1e-10 and no-penetration residual <= 1e-12
- [ ] parent Ptera state remains unchanged by diagnostic transport
- [ ] panel loads independently recompute to airplane totals and stored
  residual/tolerance ledgers
- [ ] normalized 2016-row stage ledger persists physical, tracer, self/Ptera/
  total field, storage, stability, parent, and chained hashes
- [ ] durable endpoint arrays are reopened to recompute convergence rather
  than trusting producer booleans
- [ ] final directory publication is atomic `NOREPLACE` under a race
- [ ] raw artifact freezes before opening GT for candidate scoring — BLOCKED
  BY V5H10 STOP
- [x] independent governance audit confirms diagnostic STOP and no candidate
  GT access; positive raw/artifact claims remain unsupported
- [ ] full W2 candidate — BLOCKED BY V5H10 STOP
- [ ] separately named v5h11 structure-preserving-integrator experiment is
  preregistered and passes no-GT mechanics before any paper comparison
