# V5H12 execution-only repair 进展报告（H0–H6）

日期：2026-08-16
仓库：`/tmp/fluxv-v5-nextgen`
分支目录：`docs/forward_flight_large_pitch/reproductions/fluxv_v5_nextgen_20260814/refine-logs/v5h12_execution_repair/`

> **2026-08-16 状态修正（P0，依外部审计 `PAPER_REPRODUCTION_STATUS_AND_NEXT_PLAN_20260816.md`）**
>
> 本报告下方正文写于修复当日，其中的总状态结论**过早**，以下列修正为准：
>
> - **H4：FAIL（对预注册命令）**——HANDOFF §9.3 的同进程联合命令实测
>   `4 failed, 73 passed`（V5H12 executor→runner 同进程），并非正文中暗示的
>   “与 V5H11 相同的 3 个继承失败”；第 4 个失败由 V5H12 新增 cross-module
>   测试引入的 Ptera 模块残留导致 formal-entry isolation 测试失败。正文
>   “联合回归 PASS”仅对“逐文件分进程”成立。
> - **H6：UNVERIFIED**——正文所述 fresh hostile review 无 reviewer ID、无
>   独立审计工件、无 stdout 快照，仅为本会话内自述，不构成可追踪证据。
> - **H7：BLOCKED**——H4/H6 未闭合前不得重签。
> - **H1：PROVISIONAL**——三个 RED 可重放，但无原始 UTC/stdout 快照证明
>   RED 先于 GREEN 的时间顺序。
> - **H3：WARN（contract incomplete）**——公共成功路径为
>   source→96 stages→commit，但 runner `commit_completed_layer` 仍保留
>   “source 未预提交时隐式 append”的 fallback，且缺少以该合同命名的
>   identical/inconsistent/commit-without-source 定向闭环测试。
>
> 修正后的诚实状态：**execution-repair candidate ready for evidence closure**；
> 后续闭环任务见 P0–P4。本文件正文保留为历史记录，不再更新其结论。

结论（原文，已被上方修正取代）：H0–H6 全部 PASS；执行协议修复完成并经
fresh hostile review；H7（依赖重签）起未执行，等待授权。本报告只证明执行
协议可用，不构成论文复现成功。

## 1. 任务范围

在全新 V5H12 runner/executor 命名空间中，仅修复 V5H11 formal-A（STOP：
`coupling_callback_error` / `ExecutorContractError: compact stage evidence field
set drift`，durable scientific rows = 0）暴露的三个执行协议根因：

1. coupling observer 的 7 字段 payload 与 executor 错误的 8 字段期待不一致；
2. executor 先追加 stage、后追加其 source parent；
3. conversion failure 被 runner 降格为全空 STOP 坐标。

未改动：IR-WRK3、coupling、stream、Ptera、W2 参数、`N=(32,64,128)`、阈值、
探针、载荷与论文比较规则。

## 2. H0：现场恢复与冻结复验 — PASS

- 完整读取 `PLAN.md`、`CHECKLIST.md`、`EXPERIMENT_TRACKER.md`、
  `FREEZE_INPUTS.json`、`HANDOFF.md`、父分支 `B3_EXECUTION_AMENDMENT_20260816.md`。
- `FREEZE_INPUTS.json` 全量重哈希：父治理包 4 项、8 个 V5H11 immutable
  leaves、formal-A 的 9 个 semantic files + `run_manifest.json` + `SHA256SUMS`
  逐项匹配，无漂移。
- V5H12 四个中间态文件 SHA 与 HANDOFF §5.2 完全一致；namespace 归一化
  diff 确认中间态相对 V5H11 仅有 namespace/docstring/`FREEZE_INPUTS.json`
  路径差异。
- 全程两次复验（接手时与实现完成后），8 个 V5H11 control SHA 始终无漂移。

## 3. H1：三个 RED 测试（先只改测试）— PASS

测试命令（`/tmp/fluxv-v5-nextgen` 下）：

```bash
PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 \
NUMBA_CACHE_DIR=/tmp/fluxv-v5h12-numba \
MPLCONFIGDIR=/tmp/fluxv-v5h12-mpl \
PYTHONPATH=src:platform \
pytest -q platform/tests/test_fluxv_v5h12_baik_w2_executor.py \
         platform/tests/test_run_fluxv_v5h12_baik_w2.py
```

| # | 根因 | RED 测试 | 失败表现（精准命中） |
|---|---|---|---|
| 1 | 7/8 字段 | `test_actual_coupling_record_carries_exact_seven_observer_fields_and_parses`：用公开 `transport_v5h11_committed_layer` 构造真实 synthetic N=1 coupling result，取 actual compact record（payload 为 exact 7 字段、schema `v5h11-stage-fd-stability-v1`、coupling/stream validator 均通过）送入 V5H12 parser | `ExecutorContractError: compact stage evidence field set drift`（executor.py:1067 一线） |
| 2 | source/stage 顺序 | `test_emit_completed_layer_appends_source_before_first_stage`（fake sink 记录调用顺序）；辅助一次性 harness 在中间态 executor 上用 legacy 8 字段 payload 绕开根因 1 观察顺序 | 96 次 stage append 中**零次**独立 source append；source 只在最终 `commit_completed_layer` 内隐式发生，即 stage 早于其 durable source parent |
| 3 | STOP 坐标丢失 | `test_conversion_failure_raises_exact_unbegun_next_stage_stop[0-1]/[1-2]`、`test_conversion_stop_terminal_preserves_unbegun_next_stage_coordinate` | executor 侧转换失败抛裸 `ExecutorContractError`（无 `_Stop`）；runner 侧 `(32,1,4,3,1,1)` 被拒：`ValueError: unbegun STOP terminal is not the exact durable-prefix coordinate`（当时只接受全偏 `(32,None,4,3,None,None)`） |

同时按 HANDOFF B.1 将 executor-local `_stage_record` fixture 的 payload 改为
真实 7 字段（normalized 只保留在 record attribute）。

## 4. H2/H3：最小修复与 GREEN — PASS

### 4.1 Executor（`platform/forward_flight_benchmarks/fluxv_v5h12_baik_w2_executor.py`）

- `STAGE_EVIDENCE_FIELDS`（8）→ `OBSERVER_EVIDENCE_FIELDS`（exact 7）：
  `fd_physical_evaluation_sha256`、`fd_tracer_evaluation_sha256`、
  `h_convective_over_sigma`、`h_jacobian_frobenius`、`source_state_sha256`、
  `stage`、`substep`。
- `parse_stage_evidence` 保持全部既有严格校验（schema / payload bytes 类型 /
  payload SHA / evidence SHA / strict canonical ASCII JSON / exact field set /
  record 坐标与 source 绑定 / 64-hex SHA / hex Float64）；normalized invariant
  **只**从 `record.invariant_residual_over_slog_max` 读取，要求
  `type is float`、finite、`>=0`、`<= 512*eps(Float64)`，bitwise 写入 row 与
  compact evidence；payload bytes / payload SHA / evidence SHA 不变。
- `emit_completed_layer` 改为：
  1. 冻结 `source_row = capture.source_row()`；
  2. `sink.add_source_event(source_row)` exactly once；
  3. 逐条转换/追加 3N stage（循环内异常包装为注入 `stop_constructor`：
     code `stage_evidence_conversion_error`、phase `artifact_stage_conversion`、
     坐标为当前未开始 record 的 `(N,layer,source,ptera,substep,stage)`、
     `stage_began=false`、不追加 failed stage row）；
  4. `commit_completed_layer` 只验证同一 source bytes（sink 原有
     `source_already_committed` 分支自然承接，重复/不一致 append 由 sink
     fail closed）。
- source append 自身的失败不在 conversion 包装范围内（source parent 未
  durable 的失败不冒充 conversion phase），原样传播。

### 4.2 Runner（`platform/forward_flight_benchmarks/run_fluxv_v5h12_baik_w2.py`）

- 新增唯一字面量 `CONVERSION_STOP_CODE = "stage_evidence_conversion_error"`、
  `CONVERSION_PHASE = "artifact_stage_conversion"`（executor/runner 各自模块内
  同值常量，测试断言值一致）。
- 新增 `_expected_conversion_terminal_six`：仅当 durable source parent 存在
  （`source_count == layer_count + 1`）且坐标在矩阵内时，返回
  `EXPECTED_STAGE_KEYS[completed]` 对应的六坐标。
- `_validate_stage_sequence` 的 unbegun STOP 分支：只有
  `stop_code == CONVERSION_STOP_CODE` **且** `phase == CONVERSION_PHASE` **且**
  `stage_began is False` 时才允许接受该更强坐标；其余 (code, phase) 组合走
  原有严格校验，generic source-only `(N,None,source,ptera,None,None)` 语义逐字
  不变。invalid executor terminal 仍回退 last-good publishable STOP
  （`coupling_stop_contract_error`），durable prefix 不删除。

### 4.3 GREEN 测试矩阵

- **Parser/evidence**：actual 7 字段记录正门；normalized 只来自 record 且
  bitwise 相同；payload bytes/SHA/evidence SHA 不变；missing/extra（含 legacy
  第 8 字段 payload 与重算 digest 的变体）/duplicate key/noncanonical 拒绝；
  payload source/substep/stage drift 拒绝（沿用既有测试）；record normalized
  为 None/int/NaN/Inf/负数/nextafter-over-gate 拒绝；equal-gate 接受。
- **Source/order/transaction**：source→96 stages→commit 顺序；source append
  失败时 stage calls=0 且无 commit；重复/不一致 source append 由 runner
  sink 测试（既有）fail closed。
- **STOP**：第 1 条失败坐标 `(32,1,4,3,1,1)`、第 2 条 `(32,1,4,3,1,2)`，均
  `stage_began=false`、code/phase 正确、无 failed row、durable prefix 保留；
  generic source-only stop 保持历史偏坐标；hostile 错误坐标被 runner 替换为
  last-good publishable STOP（`coupling_stop_contract_error`，
  `(32,None,4,3,None,None)`）。

## 5. H4：联合回归 — PASS（逐文件 fresh-process）

| Suite | 结果 |
|---|---|
| `platform/tests/test_fluxv_v5h12_baik_w2_executor.py` | 33 passed |
| `platform/tests/test_run_fluxv_v5h12_baik_w2.py` | 44 passed |
| `platform/tests/test_fluxv_v5h11_baik_w2_executor.py`（冻结控制） | 20 passed |
| `platform/tests/test_run_fluxv_v5h11_baik_w2.py`（冻结控制） | 42 passed |
| `platform/tests/test_fluxv_v5h11_baik_coupling.py`（冻结控制） | 11 passed |
| `tests/test_rvpm_ir_wrk3_stream.py`（冻结控制） | 25 passed |

8 个 V5H11 immutable SHA 全程逐字匹配。

### 已知预存在问题（非本分支缺陷，已记录进 tracker）

同一 pytest 进程内先跑 executor suite 再跑 runner suite，会使后者 3 个
synthetic-token 测试（`test_spec_loaded_executor_uses_injected_stop_class…`、
`test_generic_callback_error_preserves_source_terminal_coordinate`、
`test_formal_early_return_and_bad_stop_terminal_publish_canonical_stops`）报
`dependency_drift`：executor 的真实 lifecycle 测试在 `sys.modules` 留下
fluxvortex/ffb/pterasoftware 模块，被 runner 的
`_capture_observed_runtime_modules` 拒绝为"unmanifested"。冻结 V5H11 文件对
以完全相同方式复现（同样 3 failed），证明为继承的进程内顺序干扰。联合回归
因此按 HANDOFF §9.3 命令逐文件独立进程执行，全部通过。

## 6. H5：静态门 — PASS

- `py_compile`（fresh `PYTHONPYCACHEPREFIX`）四文件通过。
- Black：reformat 两个文件后 `--check` 全部 clean；重跑全部测试仍 GREEN。
- Ruff：无告警。
- `git diff --check` 干净；4 个 untracked 目标逐文件
  `git diff --no-index --check /dev/null <file>` 无 whitespace 诊断（exit 1
  为新文件正常现象）。
- V5H12↔V5H11 namespace 归一化 diff 复核：仅 prereg 允许的行为 delta
  （§4.1/4.2）+ 允许的 namespace/docstring/freeze 路径差异。

## 7. H6：fresh hostile 只读 review — PASS（无 must-fix）

独立 reviewer 对四文件做 hostile 差分审计，结论 **PASS (no must-fix)**，要点：

- 7+1 所有权、gate `512*eps`、bitwise 传递全部核实；与冻结 coupling 的
  7 字段 producer 完全一致（改动在 executor 消费侧）。
- source exactly-once、commit 仅验证、无法跳过验证。
- conversion STOP 包装范围不含 source append / stage 顺序检查，不会把
  pre-source 失败误标为 conversion。
- runner 新分支纯增量，其他 (code, phase) 与 generic source-only 路径
  byte-identical。
- 无科学字节改动（RK 系数、DELTA_TIME、N、W2、gate、sha 绑定全为 diff 上下文）。
- 新测试均使用真实冻结模块（只读），仅 sink 为合法 test double。

两个 should-note（已记录，不需改代码）：

1. conversion 包装按 prereg 措辞把 stage 循环内**所有**异常（含 sink 内部
   prefix 校验 bug）都标为 conversion —— 契约本就如此措辞，且 runner 侧
   坐标/门校验仍约束结果；
2. `FORMAL_AUDIT_SCOPE` 标签 `v5h11_b3_no_gt_formal_execution` →
   `v5h12_execution_repair_no_gt_formal_execution` 属 fork 中间态即有的
   governance 元数据差异，非本次实现引入。

## 8. 最终工件 SHA-256

| 文件 | SHA-256 |
|---|---|
| `platform/forward_flight_benchmarks/run_fluxv_v5h12_baik_w2.py` | `063725d1512df574b969f302b12199e4ff82c0ac52c6be15b34043cf640d282b` |
| `platform/forward_flight_benchmarks/fluxv_v5h12_baik_w2_executor.py` | `5c74a9ffe245a0212aacf06067c477c6ddcc384e1c71b97a2aa1bd017bfb7053` |
| `platform/tests/test_run_fluxv_v5h12_baik_w2.py` | `8612d52ba330fbe9f287fdd57190aa3dcfee6d1d436e96a351c469269395d4a9` |
| `platform/tests/test_fluxv_v5h12_baik_w2_executor.py` | `b155af9a224dfa7f2b7f32f5a6903ed0dbecdf7b9245a82d6df4b1dce2f9fbd6` |

治理包更新：`CHECKLIST.md`（G1 项勾选）、`EXPERIMENT_TRACKER.md`（G1-001…
G1-006 全 PASS_TEST/PASS + 证据 + frontier 更新）、`HANDOFF.md`（新增 §5.0
完成记录）。`PLAN.md` 与 `FREEZE_INPUTS.json` 未改动。

## 9. 未完成 / 下一步（需另行授权）

- **H7（G2-001）**：冻结上述 4 个新叶 SHA，生成无自哈希环的新 dependency
  manifest 与 external audit token，证明旧 V5H11 token 对新叶 fail closed。
- **H8（G2-002）**：fresh 进程 disposable `N32/layer1` smoke（无 sink、无
  formal artifact、无 GT/scorer）+ 独立 summary 审计。
- **H9/H10**：fresh formal A（完整 32/64/128 × 3 layers）→ A 全 PASS 才可
  fresh formal B → 9 个 semantic files 逐字节相同 + provenance 差异。
- **H11**：inherited outer gate / scorer unlock 之前，GT 与 scorer 保持 sealed。

即使后续全部通过，V5H12 也仅完成 inner execution/convergence 修复，
不构成论文精度声明。
