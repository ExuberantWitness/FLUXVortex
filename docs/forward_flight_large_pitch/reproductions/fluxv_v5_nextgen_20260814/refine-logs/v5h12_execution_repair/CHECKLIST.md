# V5H12 execution-only repair checklist

Run ID: `v5h12-execution-repair`  
Idea ID: `v5h12_execution_repair`  
Stage: `experiment`; V5H12 CONCLUDED 2026-08-17: amendment accepted (joint
regression = per-file fresh processes, evidence green); disposable N32/layer1
smoke PASS with independent audit; fresh formal A published an exact-prefix
12-file STOP at the B3 convective gate (N32/layer3/substep1/stage1,
`h*max|U-U_gal|/sigma exceeded the B3 gate`, durable prefix 2 layers + 193
stages + layer-3 source). Formal B is FORBIDDEN after a non-PASS A. The
execution-protocol repair itself is fully validated; the remaining blocker to
paper reproduction is scientific (layer-3 convective stability), requiring a
newly preregistered branch — not parameter tuning inside V5H12.

## Governance and frozen boundary

- [x] V5H11 preregistration, B3 amendment, and formal-A STOP inspected.
- [x] Baseline runner/executor/coupling/stream and focused-test hashes frozen.
- [x] Formal-A semantic files and semantic result hashes frozen.
- [x] Observation access fixed to `none`; paper scorer remains forbidden.
- [x] Scientific method, W2 inputs, N roles, probes, thresholds, and artifact
  protocol inherited without change.
- [x] Only parser/evidence ownership, source-before-stage single commit, and
  exact conversion STOP-coordinate propagation are admitted changes.
- [x] Pre-implementation amendment moves implementation to four new V5H12 paths
  and makes every V5H11 runner/executor/coupling/stream/test leaf immutable.
- [x] Source parent must be durably appended exactly once before its first
  dependent stage; no scientific bytes change.
- [x] Minimal cross-module regression, disposable smoke, full formal A/B, STOP
  conditions, and recovery rules recorded in `PLAN.md`.

## Implementation gate

- [x] Rehash `FREEZE_INPUTS.json` leaves immediately before implementation.
- [x] Mechanically fork the frozen runner/executor and matching tests into the
  four exact V5H12 target paths; do not edit V5H11 history.
- [x] In the new V5H12 executor, change observer parser expectation from eight
  fields to the exact seven coupling-owned fields.
- [x] Read `invariant_residual_over_slog_max` only from the stream compact
  record and apply the unchanged `512*eps(Float64)` gate.
- [x] Prove the parsed value written to the stage row is bitwise the record
  value; do not synthesize or rewrite observer evidence bytes.
- [x] Preserve exact active coordinates on parser/row-conversion STOP.
- [x] Confirm an empty-prefix conversion STOP is `(32,1,4,3,1,1)` with
  `stage_began=false`, not all-null.
- [x] Durably append the exact source parent before any stage append.
- [x] Ensure completed-layer commit verifies but does not append the source a
  second time; reject identical duplicates and inconsistent source commits.
- [x] Restrict the new runner's stronger unbegun-next-stage validation to the
  conversion phase; preserve generic source-only failure semantics.
- [x] Preserve every already durable prefix and emit null/empty unavailable
  evidence on STOP.
- [x] Confirm all eight frozen V5H11 runner/executor/coupling/stream and focused
  test files remain byte-identical to the freeze.
- [x] Confirm no other application/scientific file changed.

## Focused validation

- [x] Real coupling-generated compact record is accepted by the executor.
- [x] Extra/missing/duplicate observer fields fail closed.
- [x] Payload digest/schema/canonical-JSON drift fails closed.
- [x] Payload/record source, substep, and stage drift fails closed.
- [x] Record-owned invariant NaN/Inf/negative/over-gate values fail closed.
- [x] Parser STOP before the first row retains exact next coordinate.
- [x] Parser STOP after a durable prefix retains that prefix and next coordinate.
- [x] Source is present before the first stage and exactly once after layer
  completion.
- [x] Duplicate/inconsistent source append fails closed without stage append.
- [x] Generic source-only STOP fixtures retain their historical semantics.
- [x] Existing synthetic protocol tests still cannot become formal evidence.
- [x] New V5H12 executor/runner suites and frozen V5H11 control suites pass.

## Fresh execution prerequisites

- [ ] Repaired leaves frozen in a new dependency manifest.
- [ ] Fresh external audit token issued; old V5H11 token rejects changed leaves.
- [ ] Fresh disposable `N32/layer1` smoke passes with no artifact persistence.
- [ ] Smoke reports `observation_access=none`, `target_read_count=0`, and no score
  authorization.

## Formal execution

- [ ] Fresh formal A completes exact `32/64/128 x 3 layers`, or publishes an
  exact-prefix immutable STOP bundle.
- [ ] Fresh formal B runs in a separate process and destination.
- [ ] A/B first nine semantic files are byte-identical.
- [ ] A/B UUID, UTC, output path, and replicate identity differ.
- [ ] All inherited state/tracer/probe/load/stability/source/owner/artifact gates
  pass.
- [ ] Fresh read-only audit passes and issues the inherited next unlock token.
- [ ] Ground truth remains sealed until every prior item passes.

## Branch exit

- [ ] Record PASS only as an execution-protocol repair; do not claim paper
  accuracy from smoke or inner convergence.
- [ ] On any failure, archive the exact STOP evidence and end V5H12 without
  changing method, N, W2 inputs, or thresholds.
