# P2-S3: 梁-膜混合翼装配 — 实现记录(2026-07-06)

结构:真实 planform(robowing_real,nc=8/ns=16)上,主碳管梁(i=3≈30.9%c)+ 辅梁
(i=5≈69.1%c)+ 7 层板肋(j=2..14)+ 张力场膜(256 CST)+ 根部铝肋=预定扑动框架。
耦合:conforming 共享平动 DOF + ψ 独立编号(降落伞/帆/kite 主流;膜不传弯矩)。
DOF:平动 3n..3n+2 全节点;ψ 追加在 3nn 后(仅梁节点)。ndof=708。
求解:两阶段线性隐式 Newmark(逐行同 gpu_newmark_step),内层 scipy splu
(ndof~7e2 混合系统;单线程 GPU 稠密 LU 实测慢 ~100×;元素力/切线全在 Warp)。

## 门禁结果

| # | 门禁 | 结果 |
|---|---|---|
| W1 | 装配+assertion+零拟合质量 | 半翼 95.0g(膜14.7+梁56.0+肋24.3)PASS |
| W2 | **肋深标定到 K_MEAS=166.9**(单实测量→单常数,柔度进肋弯曲而非抹进膜) | d*=4.43mm → k=166.7(rel 1.3e-3)PASS |
| W3a | 预张力预平衡(软闩锁 Newton) | \|r\| 9.2→3.6e-4 N(硬判据地板 0.069N=带宽差),45/256 褶皱(自由边扇边)PASS |
| W3 | 平衡态模态体检(软一致切线) | **零负模态**;物理 f1=6.3Hz(TE 条带 bay 模态);f1(4N0)/f1(N0)=1.010 判别健康 PASS |
| W4 | host splu == GPU 稠密参考(纯梁) | q rel 4.1e-15 PASS |
| W5 | 翼 ring-down(1Pa 静垂释放) | 幅值比 1.000,主频 15.6Hz,4.6ms/子步 PASS |

## 调试史(每条都是真问题,以判别实验确诊)

1. **自由边预张力不自平衡 = 冷启动冲击**:均匀 N0 在 LE/TE 悬边残余力 ~10N 压在
   mg 级节点上,任何积分器一步即爆(与 Step-0 探针同型病灶,激励源换成预张力本身)。
   ⇒ `pre_equilibrate` 是纯结构就必须的第一步(轻量 form-finding,褶皱本构承担自由边松弛)。
2. **褶皱边界切线抖振链**(逐级确诊):
   - FD 切线固定步长在翼尖微元(边 ~2mm)上应变激励跨越褶皱边界 → 列间混支 →
     对称化出 −3e4 伪负模 ⇒ **FD 步长按单元最短边相对化**(rel_h=1e-6)。
   - 平衡态边界元素恰在 n2=0 上(物理解),逐列仍混支 ⇒ **闩锁切线**(先按状态冻结分支再 FD)。
   - 步间分支跳变使 K 阻尼算子 η↔1 跳 ⇒ 泵浦发散(动能阻尼/η 延拓均无效) ⇒
     **连续闩锁**:每步在 q_n 冻结 sigmoid 权重(k_soft=25,带宽 4%N0),步内严格变分、
     跨步连续——**即 P4 计划的 sigmoid 重参提前落地为前向光滑化**。静/动统一用软闩锁。
   - 硬闩锁静态外环分配振荡不收敛 ⇒ 软权重外环连续必收敛;残差账目分立:
     \|r\|_soft(所解问题,→3.6e-4)与 \|r\|_sharp(锐判据地板 ~0.07N=带宽系统差,如实报)。
3. **转动惯量质量缩放 ×1e4**:物理转动惯量 ~1e-11 使 M⁻¹K~1e14;该格式的隐式化
   仅经 S=M+coef·K(c_damp 实为隐式化参数,=0 时一步爆 1e13)。缩放至 ~1e-7 后
   c_damp=2 稳;物理模态污染 <1%(ρI·k²=4e-4 ≪ ρA=0.045)。文档化近似。
4. **superlu 假奇异**:S 谱跨 10 个量级(肋自由端扭转)→ 对称 Jacobi 均衡。
5. **η 残余压刚度的伪负曲率**(−0.1 N/m 级)→ μ=2 N/m Tikhonov + 5mm 步帽(只影响
   Newton 路径不影响收敛点);能量扫描守卫确认平衡为真极小(弯度鞍点假说被排除)。

## 文档化近似(累计)

膜:η=1e-4 残余压刚度;k_soft=25 连续闩锁带宽(硬判据力差地板 ~0.07N);
e1=e2 简并点(P4 sqrt(D+ε²))。梁:常参考系一致质量+转动惯量 ×1e4;加性 ψ 更新。
肋:矩形层板 3mm×d*,d* 由 K_MEAS 标定(4.43mm);GJ 薄矩形近似;G_ply=0.6GPa 文献值。

## S4 待办

WingEntry 已实现三环节预定运动+扑幅余弦 ramp;冷启动序列(冻结首窗载荷静解→
流场预发育→零速度起耦合)在 p2_s4_fsi.py;三锚点:旧 ANCF 路径(发散)/刚性 UVLM
升力 ~4.2N/实测扑动升力 ~7.79N(轻量 provider,只比升力量级)。

---

## v3 附记(2026-07-06,装配重建:平铺 + 三直杆到边缘弧,用户三轮拍板)

**物理模型修正**(用户审图指出四项,全部落地):
1. **碳杆笔直**:废除"弦向分数曲线"走向;主梁 = 直线 x=88.58mm、辅梁 = 直线
   x=198.42mm(网格线 i=3/i=5 钉在直杆上,尖部越界站位回退分数插值)。
2. **所有梁延伸到翼面边缘弧**:主梁端点 = TE 弧交点 (88.58, 792.0mm)、辅梁 =
   (198.42, 725.0mm)、LE 杆 = 翼尖 (0,800);梁端为边界上的新节点,尖部口袋区
   扇形三角闭合,梁尾段为膜边(conforming)——全部由 wing_mesh.flat_wing_mesh
   的 5 组 assertion 守护(直线性/端点在弧上/无退化三角形/梁边⊆膜边/交点共享)。
3. **结构平铺 z=0**(实机膜平绷):拱度只属于气动面——WingEntry.state() 返回
   气动面 = 变形结构网格 + NACA2406 拱度沿局部法向偏置(载荷同网格指标 1:1 传回;
   偏置转动速率对 vels 的贡献 ≤0.08 m/s ≪ U,记录忽略)。
4. **LE Ø8 实心碳杆**(论文「碳杆 8mm 翼梁」)+ 单元澄清:S1 单元本就是几何精确
   3D 梁(EA/EI₂/EI₃/GJ/剪切全在),无需更换。

**新组件**:`wing_mesh.py`(平铺网格生成器,155 节点/258 CST/103 梁元/ndof 741)、
`wing_mass.py`(参数化质量程序,co-design 质量/惯性通道:密度×真实截面×走向,
闭式可微)、`p2_assembly_fig.py`(装配图 A/D/F 由 WingModel 实例直接生成,图=代码)。

**门禁重跑(全过)**:
| 门禁 | v3 结果 |
|---|---|
| W1 | 155 节点/92 带ψ/ndof 741;质量(d*)147.8g = LE 64.0 + 主 35.7 + 辅 18.1 + 肋 15.2 + 膜 14.7 |
| W2 | **d\*=3.77mm**(旧 4.43;LE 杆分走刚度)→ k=166.7 vs 实测 166.9 (rel 1.4e-3) |
| W3 | 预平衡 8.8→**6.8e-6 N**(不动点判据修复:必须用新鲜权重残差,陈旧已解残差会提前退出留 0.21N);**0 个负模态伪影**(平铺消掉拱形 LE 鞍点,n_escape=0);f1=7.1Hz z-frac 1.000 |
| W4 | host==GPU 4.1e-15 |
| W5 | 振铃 0.999 不增长,12.5 ms/子步(旧 32ms) |

**求解器修复**:pre_equilibrate 的 outer 收敛判据从"陈旧权重已解残差"改为
"新鲜 latch 权重残差"(不动点本身 ~4×/outer 几何收敛,7 outer 到 1e-6 N;
旧判据在平铺装配上提前退出,留下 0.21 N 假地板)。

**kelvin 生产守卫**:T0 改动文件(wing_mesh/wing_mass/wing_system/p2_s3/p2_s4)
与 kelvin 路径(_v2_robo.py)零交集(git diff 证明);另钉一个配置完备的冒烟基准:
`gpu_run_twist(U=8, aoa=5, freq=2.3, tw=22.5, nc=4, ns=10, spc=40, n_cycle=5,
K0 preset)` → L=+3.1280 / T=+4.9094(旧 +5.9123/+1.1586 参考的原始配置库内无记录,
不可复现——以后回归以本条为准,±0.15N 带)。
