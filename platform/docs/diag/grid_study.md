# 网格无关性与加密方式研究(用户指示立项,2026-07-12)

背景:升力欠预测案(gap_lift.md)在 nc 收敛审计中发现生产求解器的束缚环量解在
扑动下随弦向面板数发散(§2.7)。用户裁定:**所有生产结果都应做网格无关性与
加密方式(布点律/耦合规则)研究**。本档案为正式研究记录;历史欠账:此前仅推力
在 aoa5/cosine 网格上验证过一次 nc12 网格无关性,升力/攻角维/布点律从未系统研究。

判定条件(除注明外):(U8, f2.6Hz, AoA15°, tw0),H16KTv 闭合关 LEV 剪切
(lev_shed_mode='none'),ns=16,spc=540,wake_rows=540,刚性解析运动学,
末周期均值,bern = Bernoulli 束缚力通道(2×半翼)。

## 1. 弦向面板数 nc(布点律 = cosine,生产现状)

```
              nc8     nc12    nc16    方向
扑动 bern    13.91 → 11.02 → 8.50    发散(每级 −2.5N,无收敛迹象)
静态 bern    17.22 → 16.99 → 16.92   收敛 ✓
扑动 Lkj      8.75 →  5.50 → 3.70    同步发散(排除力口径)
静态 Lkj     15.74 → 15.57 → 15.51   收敛 ✓
```

排除判定(全对照探针,详见 gap_lift.md §2.7):LEV 剪切(无剪切逐位同)、
|Cp| 钳位(cp_cap=1e9 形态不变)、老尾迹遭遇(发散对 wake_rows 平坦,60 行已全额)、
高瞬时攻角(静态 30°/45° 双口径收敛)、fp32(fp64 逐位同)。
近尾迹显微(wake_rows 0/6/12/30):发散由**最近 ~30 行渐积**。

## 2. 时间步 spc(nc12 固定,cosine)

```
spc      360     540     720    1080
bern    9.78 → 11.02 → 11.75 → 12.30   增量衰减,自下收敛 ✓(生产规则 spc=15·U·nc/f 无罪)
```

## 3. 布点律(加密方式,本研究核心判定)

弦向余弦布点(生产现状)TE/LE 面板 ∝ c·(π/2nc)²(**二次坍缩**:nc8 11mm →
nc16 2.75mm);均匀布点 ∝ c/nc(线性)。

```
扑动 bern     nc8     nc12    nc16     静态 nc16
cosine       13.91 → 11.02 → 8.50     16.92
uniform      13.90 → 13.42 → 13.56 ✓  17.55
uniform L_wind 15.48 → 14.80 → 14.99;实测 15.14 → 最恶劣角点 gap −0.15N
```

**判定:发散 = 余弦 TE 二次坍缩 × 近尾迹奇异采样的相互作用。**机理:非定常下
相邻尾迹环 ΔΓ~dΓ/dt·dt 不再对消,TE 行配置点(距 TE 丝 0.25×TE 面板)随 nc
二次坍向携带净环量的近奇异丝;静态 ΔΓ=0 全对消 → 免疫。cosine nc4/nc8 时代
距离尚大(10/2.7mm),nc12+ 进入病态区——**生产 nc12 cosine 的 28 点升力
"亏空"大部为此伪影**。

## 4. 失败的替代修复(tev_core,归档为反面教材)

"给老 TEV 行加解算液核"三版全灭(gap_lift.md §2.8):
- v1(核=0.4×四边平均):静态 16.92→3.66(展向长边把核撑到片宏观场尺度);
- v2(核=κ×流向间距,米值):静态→4.23;A/A(核=底值 0.0014)f32/f64 双路由
  同塌 4.84 → 破案:kernel δ 为**无量纲**(等效核=δ×段长),0.0014 底值在余弦
  nc16 的 0.7mm TE 距离上砍掉贴附行 TE 丝 80% 诱导,Kutta 崩塌;
- v3(新行 δ=1e-4 + 老行 δ=κ×流向/展向比):A/A 回归过(17.58),但 κ=1 静态
  再塌 5.19。**结论:亚厘米距离的近尾迹诱导是物理承载的(束缚-镜像-启动涡回流),
  解算内正则化近尾迹 = 死路。**参数保留仅作诊断。

## 5. 生产换产门(uniform × 带剪切 H16KTv × nc12,运行中)

布点换律影响 A0/LESP 提取(x_ref=0.10c;uniform nc12 LE 面板 24mm vs cosine
2.8mm)→ les 吸力/K_T/LEV 门控 → 推力链必须整体重过闸:
- 升力 4 角点(f1.4/2.6 × AoA5/15)vs 实测;
- 推力 tw0 行(f1.4/2.0/2.6)vs 实测(cosine 回放刚性误差 ~+0.6/+0.7/+0.2 参照);
- 扭转行 tw22.5@f2.3 推力+升力;
- (待)kelvin 冒烟重基线、ns 展向无关性、uniform×spc 交叉、28 点回放重跑。

**uniform 门结果(2026-07-12,带剪切 H16KTv,nc12,刚性)**:

```
升力角点         L      实测    gap   (cosine 刚性参照)
f1.4/aoa5      6.30    6.15   +0.16   (−1.00)  ✓ 伪影修复确认
f1.4/aoa15    13.43   12.87   +0.56   (−1.96)  ✓
f2.6/aoa5      5.88    7.82   −1.93   (−2.15)  ← 收敛基线上的真物理亏空
f2.6/aoa15    13.13   15.14   −2.00   (−3.62)  ← (f×α 机理,step-3 标的)
推力 tw0 行:模型斜率 +0.57N/Hz vs 实测 +1.44(cosine 曾匹配)✗
扭转行 tw22.5:gapT −3.53N(cosine ~−0.8)✗✗
```

**裁定:升力伪影修复确认、真物理亏空干净剥离(集中高 f);但推力线破门**——
uniform 的 24mm LE 面板毁了 A0/LESP→les 吸力→K_T 链(f² 推力律 + 扭转行重定向
全靠它)。换产方案升级为**混合布点 'le'(半余弦,只聚 LE)**:x=1−cos(πs/2),
nc12 时 LE 面板 2.46mm(比全余弦 4.9mm 更细,A0 更好)、TE 面板 37.5mm(比
uniform 24mm 更大,尾迹匹配更安全)。le 门组合拳运行中(升力角点+推力行+
nc 收敛复验)。

## 5.5 le(半余弦)门结果与重大判读(2026-07-12)

```
le 收敛(无剪切):nc8 14.43 → nc12 13.89 → nc16 13.80 ✓(比 uniform 更稳)
升力角点:f1.4/aoa5 +0.16 ✓  f1.4/aoa15 +1.12  f2.6/aoa5 −1.47  f2.6/aoa15 −1.94
推力:tw0 斜率仍塌(+1.08/+0.76/−0.01,模型斜率≈uniform);tw22.5 gapT −3.82 ✗
```

**关键排除:推力破门与 LE 分辨率无关**——le 的 LE 面板(2.46mm)比生产 cosine
(4.9mm)更细,推力行为却与 uniform 相同。唯一与推力结果共变的是**束缚解是否
收敛**(cosine 伪影压制束缚环量)。

**重大判读(待通道对比探针确认)**:cosine nc12 的推力"战绩"部分骑在数值伪影上
——TE 坍缩压制束缚环量,顺带吃掉扭转行 bern 后倾过阻(−3.8N 露出)与 tw0 低频
阻力(+1.1N 露出)。若坐实 = 生产闭合中存在一组**两错互抵**(纪律红线,此前
不知情),必须:换收敛网格 → 在诚实基线上重开推力案(les/K_T/geo_stall_vec 链
重验,常数不动)→ 重跑 28 点。**宁要残差可见的收敛基线,不要骑伪影的"好看"。**

## 5.6 推力通道对比终裁(2026-07-13):"闭合骑伪影"坐实,换产 le

```
              工况        T_wind  T_bern  T_les  D_vtx  T_vimp T_stall
cosine  f1.4/tw0          −1.59   +0.10  +2.48  −0.01   −0.00  −0.00
le      f1.4/tw0          −1.19   +0.16  +2.76  −0.06   +0.00  −0.00
cosine  f2.3/tw22.5       −3.95   −2.51  +2.59  +0.77   +0.40  +0.22
le      f2.3/tw22.5       −4.89   −3.95  +2.92  +0.89   +0.52  +0.40
```

- 扭转行变化几乎全在 **T_bern:−2.51→−3.95(−1.44N)**——cosine 伪影压制束缚
  环量,恰好吃掉 bern 后倾过阻(案A 的"扭转残余 −0.13N/deg"其实是伪影帮忙后的
  残值,诚实基线上 ~3×大);les/vtx/vimp/stall 合计只补 +0.67。
- tw0 侧:le 细 LE → Garrick 吸力 +0.28 → 低频阻力不足暴露(模型 f 斜率塌是
  "低频缺阻力"而非"高频缺推力":f2.6 gap −0.01 完美)。
- **终裁:换产 'le' 半余弦网格(H16KTvL)。** 宁要残差可见的收敛基线。推力案
  在诚实基线上重开:案A(bern 后倾,升为主案,诚实量级 −3.8N@tw22.5)、
  案B'(低频缺阻力 +1.1N@f1.4,f 衰减型)、案C(d_para=3.0 是 cosine 网格上
  标定的,零拟合重算加倍必要)。
- 换产执行:28 点 H16KTvL 回放重跑过夜队列运行中(录音不变,只换气动网格);
  ns 无关性预检同批;kelvin 冒烟守卫重基线待队列后。

## 6. 待办矩阵

- ns 展向无关性(ns 12/16/24 @nc12 uniform);
- LE 余弦+尾段均匀混合布点(若推力门破,兼顾 A0 分辨率与 TE-尾迹匹配);
- spc 规则 × uniform 交叉复验;
- 门禁清单永久新增一栏:**每个生产判定标注其网格收敛状态**。


## 7. 换产完成(2026-07-13)

- ns 预检(le,无剪切,f2.6/aoa15,nc12):ns12 13.32 / ns16 13.89 / ns24 14.49
  ——展向存在 ±0.6N/级 缓漂,未闭合,列入待办(比弦向病灶温和两个量级);
- 28 点 H16KTvL 回放重跑零失败(tw30 两点 le 网格解算爆掉,标 fail 入案 D);
- **新基线 MAE(柔性)**:Fig19(23 点)升力 0.74 / 推力 1.18;Fig18(10 点)
  升力 0.74 / 推力 1.89;升力全面达标,推力按诚实账本重开(§5.6);
- Fig17/18/19 已切 CFG=H16KTvL 重绘;kelvin 冒烟守卫重基线待办。
