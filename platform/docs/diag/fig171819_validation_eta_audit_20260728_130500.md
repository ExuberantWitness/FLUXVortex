# Fig17/18/19 完整验证工时审计（2026-07-28）

## 当前数据身份

- `s6_sweep_v41.json`：旧 V4.1 冻结基线，118/118。
- `s6_sweep_v4_legacy.json`：旧 legacy 冻结基线，118/118。
- 空间涡态—统一面板压力生产候选：尚未形成，因此新模型为 0/118。
- 本审计只估算验证尾段工时，不产生新模型精度或晋升证据。

## 当前机器单点实测

机器：NVIDIA GeForce RTX 4090 D。

冻结生产口径：

```text
U=8, f=2.3, nominal twist=22.5, AoA=5
nc=12, ns=16, n_cycle=4, spc=600, wake_rows=600
closure=v41
```

实测：

```text
wall = 181.79 s
L = 7.237045 N
T = 0.500641 N
```

冻结缓存为 `L=7.189566 N, T=0.476729 N`，复跑差
`Delta L=0.047479 N, Delta T=0.023912 N`，在既有 `±0.15 N`
运行带内。

## 118 工况旧求解器尾段

118 个工况的 `spc` 分布为：

```text
420:1, 480:1, 540:49, 600:15, 660:1, 720:16,
780:2, 840:15, 900:1, 1020:15, 1080:1, 1260:1
```

以 181.178 s@spc600 为锚：

- 成本正比于 `spc`：6.81 GPU h；
- 成本正比于 `spc^1.5`：7.48 GPU h；
- 保守按 `spc^2`：8.36 GPU h。

因此旧生产路径重跑一个 closure 的实测外推为 **约 7–9 GPU h**。
legacy 已有冻结缓存，若仅验证新候选，无需重复计算；若强制同时重跑两条
closure，计算量近似翻倍。

## 新模型工期表达

空间涡态、actual-boundary direct-W、统一双侧压力及 panel-force ledger
接入生产后，其相对旧路径的实测倍率记为 `kappa`。在三点计时前，不假定
`kappa=1`：

```text
new 118 compute tail = (7–9 h) * kappa
```

例如 `kappa=8–10` 时约为 56–90 GPU h，即单 GPU 2.3–3.8 天。
三点生产门完成后必须用实测 `kappa` 更新本预算。

指标计算、三图重绘与视觉核对预计少于 1 小时，不是主工时。
